import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fullsolucions.solutarquive',
  appName: 'Solut_Arquive',
  webDir: 'www'
};

export default config;
